#include <stdio.h>
int main()
{
    int a, input;
    scanf("%d", &a);

    for (int i = 1; i <= 10; i++)
    {
        // scanf("%d", &input);
        // do
        // {

        //     printf("%d ", input % 10);

        //     input = input / 10;
        // } while (input != 0);
        // printf("\n");
        printf("%d", i);
    }

    return 0;
}